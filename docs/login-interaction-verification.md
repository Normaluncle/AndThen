# 登录交互验收记录（2026-09-13）

本次范围：登录 Cookie、知乎授权返回、账号页面恢复。未修改业务表结构或资料处理同意规则。

## 自动化验收

- `pnpm typecheck`：通过。
- `pnpm test`：235 通过，1 跳过（原有真实 memU 测试，本次未调用模型）。包含数据库隔离、权限、旧 Bearer 客户端、OAuth 和 OpenAPI 检查。
- `pnpm --dir demo build`、`pnpm build`：通过。
- 最后补充缓存控制和导航位置后，定向复验 browser-login 与 OpenAPI：6 通过。
- 浏览器使用实际构建产物、真实后端和隔离 PostgreSQL，知乎响应为明确的模拟官方响应；不声称这部分为真实知乎授权。

| 用户交互 | 观察结果 |
| --- | --- |
| 账号页点击一次知乎登录 | 当前标签直接进入授权，未产生子标签 |
| 确认模拟官方授权 | 自动返回账号页，显示授权身份和成功提示 |
| 刷新整个页面 | 账号与授权状态恢复 |
| 发现和账号之间切换 | 身份保留 |
| 检查脚本及本地存储 | 无可读会话 Cookie、无存储令牌 |
| 退出并刷新 | 未恢复已撤销会话 |
| 拒绝或失效回调 | 回到账号页，提示重新授权 |

后台回归额外验证已有知乎账号重新登录仍回到原账号、回调重放被拒绝、Cookie 写请求的跨站伪造被拒绝。

脱敏交互日志与截图保留在本地 `tmp/login-interactions/`；临时脚本、截图、凭证不发布到公共仓库。

# 上线前逐项验收清单

存档提交：0a9c390bb1a1e13b0e45bb475602ba3c78c4d5f2（删除旧入口之前）

- [x] Git 存档当前版本。
- [ ] 移除旧前端入口及游客专用假数据页面；所有身份复用同一套页面。
- [ ] 首页、搜索、关注里的候选卡片均进入可刷新、可分享的详情页。
- [ ] 真实用户统计来自数据库，未知值不冒充真实统计。
- [ ] 封面字幕统一；无日期时不编日期，仍保留原回答标记。
- [ ] 工作台右侧替换指定图片，文字避开插图；横幅铺满容器。
- [ ] API 和 worker 都具备有效模型配置；真实调用 AI-A/B/C/D。
- [ ] 自动发现模型筛选、去重、额度、入库和公开候选边界逐项验证。
- [ ] 游客、读者、作者、管理员权限及主要按钮验证。
- [ ] 五问采访、保存/重试、草稿、确认发布、读者查看、撤回验证。
- [ ] 桌面及手机浏览器回归，检查图片、跳转、横向溢出。
- [ ] GitHub 同步，服务器备份、迁移、部署、健康及公网回归。
- [ ] 写明真实服务通过项、模拟通过项、未通过项和上线限制。

## 本轮本地证据

- `pnpm typecheck` 通过；`pnpm test`：249 通过，1 个独立 memory-live 环境测试跳过。
- `pnpm test:integration`：187 通过，1 跳过；新增作者同名隔离测试也已在完整测试中通过。
- `node --test demo/src/*.test.js demo/src/design/*.test.js`：42 通过；Vite 构建通过。
- 真实 qwen3.8-flash：AI-A、五问 AI-B（暂停恢复）、AI-C、AI-D、memU、确认发布、通知、权限检查通过。首次发现问题并修正后重跑，不能把早期失败算通过。
- 自动发现真实一轮：10 条官方结果，2 条模型通过入库，8 条保留审查；无作者故事自动发布。模型审核记录包含真实模型及 token 用量。
- 本机原 API 的 PID 1 陷入 Docker 内核 D 状态，无法正常替换。数据库保留；5174 的 Nginx 已指向独立的 andthen-release-api，任务由 andthen-release-worker 执行。旧容器保留，未重置 Docker 或其他服务。云端部署不使用这一本地临时拓扑。

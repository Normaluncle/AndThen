# v1.2 实施与验证记录

当前为实施中，不能作为完整交付声明。

## 已写入，待统一验收

- 作者记忆独立服务、授权/刷新、原文引用校验及采访接入。
- 官方搜索、URL 精确匹配和待补全导入。
- React 功能页面与作者工作台接口。
- 本地配置、Compose memory/demo 服务，新增数据库迁移。

## 必须继续完成

- 来源准备材料的自动触发、确认后增量更新、24 小时刷新与删除/撤销全链路。
- 官方本人内容/评论适配及 OAuth 实现和联调。
- 前端状态与契约逐页浏览器核对，管理页、真实双账号全流程。
- 项目级真实 memU 测试、故障恢复、全套回归和镜像运行验证。

## 已知外部条件

OAuth App ID/App Key 尚未提供。任意作者全文不能由目前的官方搜索保证。现有 Access Secret 本人创作列表返回空，缺少本人全文/评论真实样本。

Docker 构建在拉取 Python/Nginx 基础镜像时，Docker Hub auth token 网络请求超时；未修改系统网络，原容器保持运行。

## Git 与验证检查点

- `e4f3634`：第一阶段基础存档。类型检查通过；174 项 Vitest 测试通过；前端 build 通过；Python 3 项测试通过，含真实 embedding 批量/检索/删除测试。
- 后续真实项目链路：`MEMORY_LIVE_TEST=1 pnpm exec vitest run tests/business/memory-live.test.ts` 通过。测试使用隔离 PostgreSQL、真实 Python memU 服务、真实 Qwen 整理和提问、真实 embedding；覆盖作者隔离及撤销后不可检索。测试资料明确为 fixture，未写入真实参与者内容。
- 目标仍未完成：页面与双账号操作、更多生命周期行为以及官方适配剩余项继续执行。
- `acc72d4`：第二阶段存档，记忆生命周期作废机制与真实项目级采访联调。
- 本机隔离运行：API 8081、记忆服务 8090、页面 5173，数据库 andthen_v12_demo；原 8080 容器和原数据库保持运行。
- 浏览器已验证：读者建会话、官方搜索、关注 fixture；独立作者会话接受回访、记忆 ready、真实提问、回答保存、暂停/恢复/结束。发现并修复无请求体 POST 错发 JSON Content-Type。待继续：草稿发布、通知、撤回，以及异步状态刷新与成功反馈。

## 运行

本地配置保存在 Git 忽略的 `.env.local`。Compose 必须显式加载：

```
docker compose --env-file .env.local up -d --build
```

目标页面 http://127.0.0.1:5173，API http://127.0.0.1:8080。Python 记忆服务不发布主机端口。现有数据库卷保留。

开发规范已读取：backend-contracts、ai-evaluation、database-migrations、docker-ops、git-delivery。

## 页面闭环检查点（2026-09-13）

- 草稿未保存时禁用确认/发布；保存后新版本需重新确认。发布/撤回后读取服务端最新状态，提供明确成功提示。
- 通知阅读改为正常正文页面，不再显示原始 JSON 错误。
- 采访仅在可回答时启用输入提交；暂停/恢复/结束按钮随状态变化。后台问题更新引起版本冲突时先刷新并提示重试。
- 采访和 pending 记忆串行轮询，离开页面/开始操作后忽略旧响应，轮询错误停止。
- 独立作者和原关注读者在浏览器完成：编辑第 2 版→确认→本站发布→读者通知→读取新文字→作者撤回→读者再次读取 410。测试素材显式标为虚构 fixture。
- 验证：174 项后端测试通过、1 项 opt-in 跳过；typecheck 通过；4 项页面逻辑测试通过；Vite build 通过。
- 新增读取技能：cloudbase:web-development（用于一般 React/Vite 验证，无 CloudBase 接入）。
- 仍需继续全计划：来源准备生命周期、剩余官方接口/OAuth、管理页面、更多 memU 故障与生命周期验收、Docker 新镜像验收。

## 官方本人读取与评论同步检查点（2026-09-13）

- 已增加本人内容摘要、全文、评论适配和管理员功能页；全文按不可信文本显示，评论主页使用官方 AuthorToken。
- 评论单页后台任务、来源级去重、精确 ID 增量更新、游标校验、租约与来源删除防回写、公开授权检查、来源删除级联清理。新增迁移 0007 已应用到隔离演示数据库。
- 浏览器从项目页面真实调用本人摘要列表成功，结果为空、末页。没有取得本人全文/评论真实成功样本，保持外部样本缺口。
- 自动化：189 项 Vitest 通过、1 项真实模型 opt-in 跳过；新增官方错误、ID、分页、权限、同步事务和删除测试；typecheck 与 Vite build 通过。
- 本地 API/worker 已更新重启，原 Docker 8080 服务未变。

## 作者记忆边界与生命周期检查点（2026-09-13）

- 明确拒谈边界单独加载，不再占用五条语义检索结果的名额；向量失败仍加载有效边界。输入总预算超限时不静默丢弃边界。
- 当前材料筛选先去重并检查权限，再取最多 20 条。页面及采访回写都会排除撤回/过时的确认版本记忆。
- 记忆同意切换与清理/刷新入队处于同一事务。新索引激活前锁定来源、作者记忆并检查任务租约，避免撤销/过期后回写。
- 临时索引 PUT 超时或版本落后时排入持久清理任务；服务端 tombstone 阻止已删除版本复活。
- 相同输入复用索引并更新时间；作者返回工作台超过 24 小时会后台排入去重刷新。自动同意过期及私有资料保留期清理会作废记忆索引。
- 成功的 memory.refresh 任务结果记录 model_id、llm_calls、input_tokens、output_tokens、llm_latency_ms、index_writes；缺少 token 统计保留 null，不伪装为零。失败任务记录与向量调用统计仍需进一步补齐。
- 2026-09-13 03:23（运行环境时钟）真实 memU 项目测试通过，整理部分为 qwen3.8-flash 两次调用、226 输入 token、178 输出 token、2999ms、一次索引写入。采访调用另计；此单次结果不构成性能保证。
- Python 三项测试再次通过，包括 22 条真实 embedding 分批写入、重开检索、删除后拒绝重新创建同一版本。

### 后续实现必须保留的缺口

此处记录上一检查点的缺口；下方“官方候选准备检查点”已补齐其中的关注和来源准备通路。不要把已有 fixture 故事的双账号闭环等同于真实作者自然发现链路全部完成。

拟继续使用服务端保存的官方候选记录（避免客户端伪造摘要），按官方 URL 的来源范围保存准备材料；首次关注才触发，复用相同材料版本，不按昵称建档。后续作者需核验该来源、同意处理后才能使用相应准备材料。还需：完整管理页面、OAuth 实现/外部凭证、模型失败与同作者任务并行控制的扩展测试、Docker 新镜像验收。

- 此记忆检查点最终回归：pnpm typecheck 通过；pnpm test 为 195 通过、1 项 opt-in 跳过。真实 opt-in 已另行运行通过，不能把默认跳过当失败或把模拟测试当真实模型测试。
- 同一草稿版本的重复确认已改为幂等，不重复生成记忆版本；有针对性回归。
- 本地 8081 API 与隔离 worker 已更新到此检查点代码，原 8080 容器服务保持原样。

## 官方候选准备检查点（2026-09-13）

- 新增迁移 0008：官方候选缓存和来源级准备版本；已应用至隔离演示数据库。搜索不建档，首次关注后台整理，重复关注复用；作者核验和处理同意前不归入人物记忆。
- 发现与我的关注页面可以直接关注/取消官方候选。服务端读取已保存的官方摘要，拒绝客户端伪造正文。按来源版本保留逐字依据，来源删除安排索引清理。
- 真实浏览器官方搜索“转行”后关注一条官方摘要，后台 Qwen + memU 成功得到四条来源准备记录。该来源没有任何核验作者。取消再关注后数据库仍为一个准备任务，原 ready 版本复用。
- 此次真实整理为一次 Qwen 调用，445 输入 token、312 输出 token、4830ms；不含向量耗时/用量，不构成速度或质量保证。未联系真实作者，未把测试账号冒充成原作者。
- `discovery-preparation.test.ts` 在真实隔离 PostgreSQL 中覆盖两读者重复关注、同名不合并、未核验/未同意不关联、核验且同意后复用材料、取消及来源删除清理。测试中的官方/模型服务为模拟；上述浏览器记录另用真实服务。
- 当前全套回归 196 通过、1 项真实模型 opt-in 跳过；typecheck 与 Vite build 通过。真实 memU 项目测试在上一检查点独立通过。
- 仍需补齐管理页面、完整失败任务操作、OAuth 实现和凭证联调、更多生命周期故障/同作者串行测试、Docker 新镜像验收。整体目标继续保持 active。

### 来源准备失效补测

- 新材料版本入库、撤销采访/模型处理/公开展示同意时，来源准备版本立即失效并安排索引清理。已撤销或拒绝的来源禁止再次导入。
- 一旦有核验作者，后续来源准备必须检查其采访及模型处理同意；不能借“公开摘要准备”绕过该作者的撤销决定。激活前再次检查最新快照和同意。
- 模拟索引写入中断：状态为 error，无已启用记录；再次关注产生新版本，即使旧任务仍占有租约也不会吞掉新任务。模拟写入期间撤销同意：旧任务不能写回，重复关注不重新建档。新材料替换后，旧官方候选不能复活旧快照准备。
- 此检查点保留的后续项包括核验后审核通路、完整管理页面及自动恢复策略；下面记录审核通路的后续实现。来源准备成功不等于自然作者闭环已经全部完成。

### 导入来源核验后的回访衔接

- 已核验作者同意私有采访后，pending 来源变为 private_only；不会因此公开。第三方链接不再被永久拒绝审核，必须核验归属且取得该作者的采访同意后才可通过审核。
- 核验在来源行锁内检查冲突、登记记录及关联回访。并发核验给两个账号时，一个成功、另一个冲突。先核验再由运营创建回访，也会关联已有核验作者。
- 新增隔离数据库测试：未核验不能代替作者同意、核验但未同意不能审核、同意后审核及接受并开始采访、原内容仍不公开；并发核验不产生两个所有者；核验在先的回访出现在正确作者工作台。
- 管理页面仍需补齐，不能把这些 API 测试报告为已完成浏览器自然作者流程。
- 此检查点 `pnpm typecheck` 通过；`pnpm test` 为 201 通过、1 项真实模型 opt-in 跳过，包含新增的来源准备失效、核验并发及私有回访衔接测试。

### 回访管理功能页

- 新增来源管理列表、材料版本阅读、已有作者核验与同意查看；通过原有接口记录核验、创建回访、按当前版本审核和登记人工触达。管理人员不能替作者签署处理同意，页面不会向外部发送消息。
- 管理员可创建 team_test 作者账号，凭证仅保留在当前页面内存，主动清除或离开页面后不再显示。创建账号不等于核验作者。
- 管理员失败任务列表只返回任务类型、状态、次数、时间和编号，不暴露 payload/result/上游错误。来源列表按管理员或负责研究员权限过滤。通用重试仍未开放，需要对应业务入口重新校验条件。
- 浏览器通过隔离管理员会话验证：列表显示真实官方来源准备 ready；打开已有 fixture 可看材料版本、核验和处理同意；失败任务为空时正确显示空态。核验/审核写操作已有 API 回归，但新页面写操作浏览器验收仍待补齐。
- `management.test.ts` 两项真实数据库测试通过，覆盖匿名/普通读者拒绝、研究员范围、管理员失败任务字段隔离；typecheck 和 Vite build 通过。API 8081 与隔离 worker 已更新，原 Docker 8080 未改动。
- 浏览器负例：尝试对已撤回的 fixture 回访再次审核，得到 conflict 且状态仍为 withdrawn；未对真实作者内容执行归属或审核变更。
- 最终回归 `pnpm test`：203 通过、1 项真实模型 opt-in 跳过；运行中的 OpenAPI 已包含两个 operator 路径。

### AI 草稿与依据检查页面（2026-09-13）

- 草稿页新增 AI-C 整理新版本与 AI-D 依据检查。任务执行期间禁止编辑/确认/发布；离开页面停止轮询结果回写。读取状态失败可重新读取，处理失败可重新发起；原回答和草稿保留。
- 区分队列 succeeded 与模型结果成功：`error_code`、缺少 draft_id/validation、failed/cancelled 都不能被展示为有效结果。成功整理后加载新的未确认版本。规则检查单独说明，不能冒充 AI 检查。
- 浏览器实测先发现 Qwen 将采访 message 标为 source_quote，服务端以 source_incomplete 拒绝且第 3 版原稿保留。增加提示词中的引用类型映射，版本更新为 2026-09-13.2，证据校验没有放宽。`drafting.test.ts` 新增错误引用类型的回归场景。
- 同一 fixture 重试成功：第 4 版有六条有依据内容、状态 draft，尚未确认或发布。随后 AI-D 成功，页面显示“未发现阻断项，仍需作者确认”。这是真实模型的测试材料验收，不是真实作者案例或广泛质量保证。
- 项目 ai_runs：首次 AI-C 失败 439/372 输入/输出 token、3733ms；重试 AI-C 成功 569/658、6739ms；AI-D 成功 873/16、702ms。另有两次诊断探针，分别 439/275 和 569/658 token；本轮总计五次真实模型请求，2889 输入、1979 输出 token。探针不是产品验收替代品，未写入业务草稿。
- 验证：typecheck 通过；pnpm test 203 通过、1 项 opt-in 跳过；页面 API/流程测试 6 通过；Vite build 通过。隔离 worker 已加载修订提示词。
- 仍需：原始依据正文的便捷查看/逐项编辑操作、管理页正向写操作及自然发现完整浏览器闭环、采访失败重试、记忆与失败任务完整恢复操作、OAuth 实现及外部凭证、Docker 新镜像等原计划剩余项。Goal 不标完成。
